# Superseded plain-language draft of the terms, kept locally for reference.
# NEVER commit: GitHub Pages renders any .md in this repo as a page, so
# committing it would publish a second, contradictory terms document at a
# guessable URL.
index.plain-language.bak.md

.DS_Store

# Firebase admin credentials — this repo publishes via GitHub Pages, so a
# committed key would be world-readable at a guessable URL. Never commit.
firebase_admin_sdk.json
sa.json

# Local environment file. Empty today, but this repo is PUBLIC and publishes
# via GitHub Pages — anything committed here is world-readable at a
# guessable URL. Ignored so a wide `git add` cannot publish it later.
.env
.env.*
